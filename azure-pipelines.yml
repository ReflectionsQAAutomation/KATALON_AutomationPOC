
trigger:
- master

pool:
  vmImage: 'windows-latest'

steps:
- task: katalonTask@1
  inputs:
    version: '8.6.8'
    executeArgs: ' -retry=0 -testSuitePath="Test Suites/Smoke_EndToEnd" -browserType="Remote" -executionProfile="default"-apiKey="3xdmwm5geylksza7juf6eeho5tyh2ps4khm57gt3hdcbdqli4bna" --config -proxy.auth.option=NO_PROXY -proxy.system.option=NO_PROXY -proxy.system.applyToDesiredCapabilities=true -webui.autoUpdateDrivers=true'
